server:
  port: 9004
spring:
  application:
    name: ORDER-SERVICE

  rabbitmq:
    addresses: localhost:5672

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    hostname: localhost
